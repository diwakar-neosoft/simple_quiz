<%= form_with(model: admin_subject, html: {class: 'mx-auto', style: 'max-width: 450px;'}) do |form| %>
  <% if admin_subject.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_subject.errors.count, "error") %> prohibited this admin_subject from being saved:</h2>

      <ul>
        <% admin_subject.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-group row'>
    <%= form.label :name, 'Name: ', class: 'col-2' %>
    <div class='col-10'>
      <%= form.text_field :name, class: 'form-control' %>
    </div>
  </div>

  <div class='form-group row'>
    <%= form.label :num_of_questions,'Num of questions: ', class: 'col-2' %>
    <div class='col-10'>
      <%= form.number_field :num_of_questions, class: 'form-control' %>
    </div>
  </div>

  <div class="row">
    <%= form.submit 'Submit',class:'btn btn-primary col-12' %>
  </div>
<% end %>
